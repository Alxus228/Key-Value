<!DOCTYPE html>
<html>
    <head>
    <title>Key-value data</title>
    <style>
        td, th {
    	    border: 0.5px solid;
        }

        table {
            width: 100%;
            border-collapse: collapse;
        }
    </style>
    </head>
    <body>
        {{if .}}
        <h3><i>Key-value storage content:</i></h3>
        <table style="width:100%">
            <tr>
                <th>Key</th>
                <th>Value</th>
            </tr>
            {{range $key, $value := .}}
                <tr>
                    <td><strong>{{$key}}</strong></td>
                    <td>{{$value}}</td>
                </tr>
            {{end}}
        </table>
        {{else}}
        <p><i>Key-value storage doesn't have any content</i></p>
        {{end}}
    </body>
</html>